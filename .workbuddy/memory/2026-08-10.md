# 2026-08-10 工作日志

### 03_ShortETF 全行业池改造 + 增量更新 + 分位bug修复（00:00-00:30）
- **用户要求**：池子=全部行业ETF（不筛规模/成立时间）；删旧库重建；每次运行增量更新+出日报
- **新增 fetch_all_industry.py**：腾讯前复权接口拉全部 747 只行业ETF（从 market_all.db 清单 is_industry 过滤），输出 data/market_all_qfq.db（688只/53.6万行K线，96秒）；支持 --incremental（拉最后日期之后的新K线，限速0.2s防WAF）
- **sector_report.py 改造**：
  - DB 改 market_all_qfq.db（全行业池，去掉 mcap>=20 筛选）
  - main 加 0/4 步 ensure_market_updated()：智能判断（库内最新日期<最近交易日才增量拉取，避免每次请求接口）
  - **修 pct250 bug**：theme_aggregate 原来 `(x['pct250'] or 0)` 把 K线<250根的次新ETF的 None 分位当 0 参与平均 → 假"0% 低估"；改为仅有效值求均值，全无效显示"—"
- **回答用户两个问题**：① 持仓分位 0.2/0.2/0.4 是真实值（地产17%/食品24%/消费38%四舍五入，行业近期回调处历史低位）；② 低估雷达大量0 是次新ETF K线<250根被 or 0 误判的 bug（科创芯片设计87根/港股通信息技术31根/航空航天2根）
- **重要教训**：腾讯 fqkline 接口有 WAF 风控——全量688次连续请求触发501拦截（约30分钟+才解封）；批量行情接口 qt.gtimg.cn 不受影响。以后全量/增量都要限速（0.2s+间隔）
- 删除旧库 data/value_bt.db；market_all_qfq.db 数据到 2026-08-07（最新）

### 主库统一 + 全面清理（00:17-00:45）
- **用户要求**：干净全面的本地数据库（后续可能增加ETF、买入卖出量等）；删除所有废弃文件
- **回答 1623 vs 688**：market_all.db 是全市场 1623 只清单（行业型747 + 宽基475/其他230/跨境66/债券52/货币45/商品8）；market_all_qfq.db 只拉了行业型（688成功，59只次新失败）
- **新增 lib/signal_lib.py**：从 run.py 提取 theme_of/is_industry/classify_industry/macd_series/FUND_COMPANIES/INDUSTRY_CLASS/EXCLUDE_KW（sector_report 与 fetch 共用）
- **统一主库 data/market.db**：etf_meta 全市场1623只（含 type 类型标注 + is_ind 标记）+ kline（code,date,open,close,high,low,volume,amount 8字段支持买入卖出量）；迁移原688只K线（date/close，OHLCV 待 WAF 解封后 --full 补全）
- **新增 fetch_db.py**（取代 fetch_all_industry.py）：智能更新（无K线拉全量/有K线增量）、--full 重拉、--incremental、--types 按类型扩展；限速0.2s + WAF检测（连续10次失败提前终止）
- **sector_report.py**：DB→market.db，from signal_lib import 替代 from run import，ensure_market_updated 调 fetch_db.py
- **删除**（释放124MB）：run.py、fetch_qfq.py、fetch_all_industry.py、lib/batch_api.py、lib/top100_etf.json、lib/etf_momentum_cache.json、data/etf_strategy.db、data/market_all.db、data/market_all_qfq.db
- **最终结构**：sector_report.py + fetch_db.py + lib/(signal_lib, otc_map) + data/market.db(50MB) + Sector-log.csv + reports/
- **提醒**：腾讯 fqkline 接口 WAF 风控仍在（501），全量补 OHLCV 需等 30-60 分钟解封后跑 `python fetch_db.py --full`；日常增量不受影响（数据到 08-07 已最新）

### WAF 解封，全量补齐 OHLCV 完成（14:30-14:35）
- 腾讯 fqkline 接口解封（status 200，返回6字段[date,open,close,high,low,volume]，无第7个成交额字段）
- **fetch_db.py --full 全量重拉**：3分12秒完成，**747/747 全部成功（0失败，含之前59只失败的次新）**，57.1万行 K 线，volume 100% 非空，最新日期 2026-08-10
- amount（成交额）字段腾讯接口不返回 → 保持 NULL，字段保留待以后扩展
- 效果：日报行业型 600→**655只**/主题 160→**169个**；持仓 3 笔全部有预警（此前地产缺数据无预警）；低估 57 / 强势 21
- 注意：sh512200 等个别标的可能缺最新一天数据（停牌/接口差异），正常

### 持仓改 .log + 综合评分体系（14:40-15:00）
- **用户把买卖记录改成 Sector.log**（.log 文本，混合分隔：表头tab/数据行空格+tab；列=日期(YYYYMMDD) 基金 代码 方向 金额 收益，新增收益列）
- **sector_report.py 重写**：
  - load_positions 改为解析 .log：split() 任意空白分割，尾部固定[收益,金额,方向,代码]，中间=基金名（可含空格）；跳过表头；日期 YYYYMMDD
  - otc_to_theme 支持 OTC_MAP 值结构 tuple 和 list-of-tuple（创新药改成双基金列表：工银013011/013012 + 国泰014117/014118）
  - **新增综合评分体系**（满分100，权重 WEIGHTS 常量可调）：20日动量30 + 5日10 + 60日10 + MACD多头/红柱15(衰竭减半) + MA60站上/偏离15 + 历史分位低估10(分位越低分越高) + 资金流10
  - **资金流指标**：量能方向比（近N日 上涨成交量-下跌成交量）/总成交量，flow5/flow20，字段 -100~+100%
  - load_market 改读 open/volume（OHLCV 已全）；theme_aggregate 加 flow20 均值
  - 报告：排名表加"资金流"列，评分→综合分（主题排名+持仓表+代表显示），note 说明权重
- **otc_map.py**：创新药改为 list 结构加国泰联接（014117/014118，用户新买入014118）
- **效果**：4笔持仓全识别（含新增创新药¥200）；排名前5=黄金股86/港股医疗85/港股医药84/港股通医疗84/医药50 84（动量强+资金流入）；创新药综合分80（分位76%偏高预警+双多）
- README 更新：Sector-log.csv→Sector.log、评分→综合分、记录格式

### 持仓表修复：分位格式 + 去代表ETF列（15:02-15:05）
- **bug**：持仓预警表历史分位用 fmt_pct(0.2,'%') → 显示 "+0.2%"（应为 20%）。修复：分位改用 `{pct:.0%}`（0.2→20%）
- 持仓表去掉"代表ETF"列（用户不需要）；低估/强势/排名雷达的代表ETF列保留（方向参考用）
- **确认综合分数据来源**：完全用场内行业ETF的K线计算（market.db），非场外基金数据——流程：场外代码→otc_map→主题→主题内ETF聚合（均值+综合分最高者为代表）。无需重算

### 主题排名展示全部（15:09）
- 用户要求：全行业排名展示全部（原 themes[:60] 截断），便于看资金流向（排名=强弱，资金流列=净流入/流出）
- 改为 enumerate(themes, 1)，169 主题全展示

### 统一持仓流水库 + Web管理界面（15:22-15:45）
- **用户痛点**：Excel/CSV 会把代码列转数值丢前导零（004643→4643）；Index.log 里已有实例（21457 应为 021457，已修复）
- **统一库 data/portfolio.db**（根目录）：trans 表（id/date/category/fund/code TEXT保前导零/direction/amount/gain/note），三类流水一张表：指数/行业/资金池
- **迁移**：02_IndexETF/Index.log（指数10条）+ 03_ShortETF/Sector.log（行业4条）→ 库；修复 21457→021457
- **portfolio_app.py**（根目录，Web界面，标准库http.server无依赖，端口8051）：流水列表+类别筛选+搜索；新增表单——指数下拉（6国内+标普019305+纳指019172，选基金自动带代码防丢零）/行业自由输入/资金池固定余额宝(ZFB)方向转收支；编辑/删除；汇总KPI（指数/行业/余额宝/总资产）
- **sector_report.py**：load_positions 改读 portfolio.db（category=行业），Sector.log 仅作历史
- **export_positions.py**：从库生成根README第4节组合持仓表（--write 直接更新），收益率列保留README旧值
- **效果**：指数¥5996.80(6只)/行业¥1200.01(4只)/总资产¥7196.81，与README手工表吻合；日报4笔持仓正常
- README 更新：根README顶部+第4节、03_ShortETF（持仓管理改 portfolio_app.py）
- 余额宝资金池：库里暂无记录（用户README显示ZFB 9000现状），待用户在Web界面录初始余额

### portfolio_app 三处修复（15:49-16:00）
- ① **行业基金可自由输入**：基金字段改为动态——指数/资金池=下拉（自动带代码），行业=文本框+datalist 提示（从库内已有基金名收集）；代码字段行业可编辑
- ② **加当前持仓表**：KPI 下新增卡片，按类别分组显示 基金/代码/当前持仓/累计成本
- ③ **修复编辑**：原来用 /api/trans?q=id 模糊搜索匹配不上 → 后端 list_trans 支持 tid 精确查询（/api/trans?id=N），前端 edit() 用精确查询+buildFundField 重构填充
- 后端加 /api/names?category= 接口（datalist 数据源）
- 用户已在界面录入余额宝转入10000（README原9000→以库为准）；export_positions.py 修复资金池类别 KeyError（归入"其他"）
- **踩坑**：重启服务器时旧进程没杀干净导致双进程抢8051端口（curl 0B/异常）；Git Bash 杀 Windows 进程需 MSYS_NO_PATHCONV=1 taskkill /PID

### 资金池推算余额 + EXE 打包（15:54-16:05）
- **资金池关联**：用户说明 8/7 转入 10000 后基金买入都从池子出。get_summary 加 cash_implied 推算余额 = 资金池手工余额 - 自首次转入日起(指数+行业买入-卖出)；界面 KPI 加"余额宝推算余额"卡（当前 10000-1200.01=8799.99）
- **web vs exe 评估**：managed python 无 tkinter、PyQt 体积大 → 选定 PyInstaller 打包 Web 内核为单文件 exe（双击→自动开浏览器→关窗口即停），保留现有界面、不依赖 python 环境
- **打包**：pip install pyinstaller → pyinstaller --onefile --console；路径逻辑（frozen 时 BASE=exe目录，DB 候选：exe旁data/ → 项目根data/ → cwd data/）
- **大坑**：旧 exe 进程占用导致 PyInstaller 打包"成功"但没写入新代码（时间戳不变）→ 症状是跑了旧代码一直 unable to open database；解决=杀进程+删旧exe+重打包。exe 路径 D:\CodeX\InvestmentLearningJourney\portfolio_app.exe（9.1MB，16:03 新版验证通过：总17196.81/推算8799.99）
- 清理 build/；portfolio_app.exe 留在根目录 + dist/ 各一份

### 资金池转入记录补全（16:11）
- 用户说明资金时间线：7/3转入1000→买沪深300；7/10转入3000→买三笔指数；8/3转入2000→买六笔指数；8/7转入10000→买行业+创新药
- 库内资金池改为4笔转入（原仅8/7一笔10000）：合计¥16000；推算余额=16000-全部买入7196.81=**¥8803.19**（首转入日7/3早于所有买入，扣全部）
- 总资产=16000+5996.80+1200.01=¥23196.81；README持仓表已更新

### 持仓表资金池显示修正（16:16-16:26）
- 用户指出：持仓表资金池显示转入累计16000不对，应显示可用现金（推算余额8803.19）
- portfolio_app.py：持仓表资金池行改用 cash_implied（可用现金）；**修复总资产重复计算 bug**——原来 16000(资金池手工)+5996.8+1200.01=23196.81 把已买部分算两遍；改为 总资产 = 推算余额 + 指数 + 行业 = 16000 ✓
- export_positions.py：README 表余额宝行显示推算余额（import portfolio_app 复用 cash_implied）
- 重新打包 exe（16:26，验证：推算8803.19/总16000）

### 流水顺序重排 + ID重编（16:27）
- 用户指出：必须先转入资金池才能买入，原流水买入排在转入前逻辑错误；ID 也要重排
- 重建 trans 表：按(日期, 类别优先级[资金池0<指数/行业1], 原ID)排序，ID 从 1 重新连续编号
- 结果：id1=7/3转入1000 → id2=7/3买沪深300；id3=7/10转入3000 → id4-6三笔；id7=8/3转入2000 → id8-13六笔；id14=8/7转入10000 → id15-17行业三笔；id18=8/10创新药
- 汇总不变：指数5996.80/行业1200.01/推算8803.19/总16000（exe 无需重打包，读同一db刷新即见）

### 项目合并到根目录 + 全面清理（16:31-16:50）
- **用户要求**：备份记录代码；02/03 代码+数据+lib 合并到根目录；清理编译产物与无用文件
- **合并结果**（02/03 目录只剩各自 README.md）：
  - 根目录：portfolio_app.py(+exe) / export_positions.py / sector_report.py(03主程序) / fetch_db.py(03数据) / index_dashboard.py(02看板,原script.py)
  - lib/：signal_lib.py, otc_map.py(03) + db.py, dca.py(02)
  - static/：02 看板静态文件
  - data/：portfolio.db(持仓流水) + **market_index.db**(02指数行情,原02/data/market.db) + **market_industry.db**(03行业行情,原03/data/market.db)——两库表结构不同不能合并，改名区分
  - backup/：portfolio_app_backup.py + 旧 Index.log/Sector.log
- **路径改动**：sector_report.py(PORTFOLIO_DB→data/portfolio.db、DB→market_industry.db)；fetch_db.py(DB→market_industry.db)；lib/db.py(DB_PATH→market_index.db)
- **清理**：dist/build/*.spec/全部__pycache__/测试日志/exe残留/03旧sector_report.html/03_reports/旧log
- **验证**：日报正常(655只/169主题)、02看板8050 OK、持仓管理8051 OK
- **重要认知**：02 和 03 的 market.db 表结构完全不同（indices估值表 vs etf_meta/kline），"合并数据库"实际=改名分文件放同一 data/ 目录

### 重组：Code目录 + 根目录只留入口 + 报告回02/03（16:49-17:00）
- **用户要求**：代码和数据统一进 Code/ 目录；每天运行的放根目录；报告放 02/03 各自目录
- **结构**：
  - Code/：portfolio_app.py / export_positions.py / index_dashboard.py / sector_report.py / fetch_db.py + lib/ + static/ + data/(portfolio.db, market_index.db, market_industry.db) + backup/
  - 根目录：仅 portfolio_app.exe（每天双击）+ README.md + requirements.txt + 02/03 目录
  - 03_ShortETF/sector_report.html（行业日报报告输出位置）
- **路径改动**：sector_report OUT_HTML→../03_ShortETF/；portfolio_app frozen 候选加 Code/data/；export_positions README→../README.md
- **重新打包 exe**（16:51，根目录 9.1MB）：验证 推算8803.19/总16000，找到 Code/data/portfolio.db
- **注意**：移动文件后运行中的旧进程路径失效（8050 旧看板 static 404）→ 需重启；PyInstaller 打包前必须先杀运行中的 exe（覆盖失败坑）

### 持仓管理界面精简（17:09）
- 用户要求：去掉"余额宝余额"KPI卡（转入累计无意义），只留 指数/行业/余额宝(可用现金)/总资产 四张
- 持仓表：指数/行业按当前持仓金额降序，余额宝固定放最后
- 重新打包 exe（17:10），验证：kCash 卡已删、kCashImplied(可用现金)在、排序逻辑在

### 去除收益字段（17:18）
- 用户要求：新增/编辑表单和流水明细表去掉"收益"列
- 前端删除 fGain 输入框、表头<th>收益</th>、行 t.gain td、edit/reset/save 的 fGain 引用；grid 8列→7列
- 后端 gain 字段保留（p.get('gain') or 0 默认0），数据库不动
- 重新打包 exe（17:20），验证三处收益引用全无

### 最终目录结构：run.py统一入口（17:28-17:45）
- **用户要求**：目录英文命名；02→指数报告 03→行业报告；报告只在对应目录；exe入Code；根目录run.py统一调用
- **最终结构**：
  - 根目录：run.py（统一入口）+ README.md + requirements.txt（极简）
  - Code/：portfolio_app.py+exe / sector_report.py / index_dashboard.py / fetch_db.py / export_positions.py + lib/ + static/ + data/(portfolio/market_index/market_industry.db) + backup/
  - 02_IndexReport/（指数报告目录）、03_SectorReport/（行业报告目录，sector_report.html 输出地）
- **run.py 参数**：--index（看板8050）/ --sector（行业日报）/ --portfolio（流水8051）/ 可组合
- **exe 路径候选调整**（放 Code/ 后）：exe旁data/ → 上级/Code/data/ → cwd data/；exe 17:40 打包入 Code/
- **教训**：中文目录名会导致代码路径识别问题，必须英文命名

### 去掉累计成本列 + README自动同步（18:10-18:25）
- **portfolio_app.py**：持仓表去掉「累计成本」列（表头+数据行）；KPI 不变
- **export_positions.py 重写**：输出与持仓管理同口径的 4 列表（类型/代码/当前持仓/基金），指数/行业按金额降序、余额宝最后显示推算余额；新增 write_readme() 可复用；README 第4节已更新（指数 8 只含纳指标普、合计 6096.80；行业 4 只 1200.00；余额宝推算 8750；总计 16046.80）
- **README 自动同步两条路径**（用户要求"加到 run 里 + 记录后自动更新"）：
  ① run.py 新增 --export / -e → 调 export_positions.py --write
  ② portfolio_app 增删改后自动调 sync_readme() 更新 README 第4节（内联实现，避免 PyInstaller 动态导入问题；需 re import）
- **验证**：加→删流水 README 完全复原；exe（18:23 版）通过 HTTP API 增删也同步正常
- **Code/ 代码盘点**（全部在用）：portfolio_app(流水+exe) / sector_report(行业日报) / index_dashboard(指数看板) / fetch_db(行情维护) / export_positions(README表) / lib(signal_lib+otc_map+db+dca) / static(看板前端) / backup(备份)
